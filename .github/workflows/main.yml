name: ansiblecd

on:
  push:
    branches: [main, devel]

jobs:

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt -y install ansible python3-kubernetes
      - run: KUBECONFIG=config.yaml ansible-playbook devel.yaml
